[pytest]
asyncio_mode = auto
testpaths = tests
# Hard per-test ceiling so a hang (e.g. an asyncio teardown that never
# resolves) fails loudly in seconds instead of consuming CI minutes silently.
timeout = 30
